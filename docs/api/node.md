---
title: Node
description: API reference for revvlink.Node — a single Lavalink server connection.
---

# Node

A `Node` represents a single Lavalink server connection managed by the `Pool`.
Create `Node` instances and pass them to `Pool.connect()` — you do not connect them directly.

!!! tip "Guide"
    See the [Node & Pool Guide](../guides/node-pool.md) for usage patterns and multi-node setup.

---

::: revvlink.Node
